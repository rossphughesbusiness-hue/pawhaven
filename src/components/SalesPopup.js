'use client';

/**
 * Removed: this component fabricated "recent purchase" notifications with
 * invented names/cities. Deceptive social proof — permanently disabled.
 */
export default function SalesPopup() {
  return null;
}
