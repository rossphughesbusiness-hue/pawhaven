/**
 * Seeded reviews have been permanently removed.
 * PawHaven only displays real, user-submitted reviews (see /api/reviews).
 */

export const SEEDED_REVIEWS = {};

export function getSeededReviews() {
  return [];
}
